#include <iostream>
#include <algorithm>
#include <vector>
#include <string>

using namespace std;




void solve(){
    int N, M;
    cin >> N >> M;
    
    bool arr[N+1] = {false, };
    vector<int> v[N+1];

    for(int i = 0; i < M; i++){
        int a, b;
        cin >> a >> b;
    }

    
    cout << N -1 << endl;

    
}

int main(){
    int T;
    cin >> T;
    
    while(T--){
        solve();
    }
    

    return 0;
}